import '../../styles/Case.css'

function AllerPrison({id, image}) {
    return (
        <div className='allerPrison' key={id} id={id}>
        <div className='gridJoueur'></div>
        </div>
    )
}

export default AllerPrison;